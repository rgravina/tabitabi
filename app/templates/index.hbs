<br/>
<div class="row">
  <div class="col-sm-8 col-sm-offset-2">
    <div class="jumbotron">
      <p>Welcome to <strong>TabiTabi!</strong></p>
      <p>TabiTabi will help you increase your vocabulary and reading ability in Japanese.</p>
      <br/>
      <div class="row">
        <div class="col-sm-6 text-center buttons">
          <button {{action "signIn" "facebook"}} href="#" class="btn btn-primary btn-lg">
            {{fa-icon "facebook" size="lg"}}
            Login with Facebook
          </button>
        </div>
        <div class="col-sm-6 text-center">
          <button {{action "signIn" "twitter"}} href="#" class="btn btn-primary btn-lg">
            {{fa-icon "twitter" size="lg"}}
            Login with Twitter
          </button>
        </div>
      </div>
      <div class="row text-center">
        <div class="col-sm-12">
          <a href="#" {{action 'toggleShowWhyLogin'}}>Why login?</a>
        </div>
      </div>
      {{#if showWhyLogin}}
      <div class="row">
        <div class="col-sm-12">
          <div class="well">
            <p class="text-center">Logging in allows you to:</p>
            <ul>
              <li>Keep using your account after you have cleared your browsers local storage.</li>
              <li>Use the same account on the web and mobile app.</li>
              <li>Participate in discussions.</li>
            </ul>
          </div>
        </div>
      </div>
      {{else}}
        <br/>
      {{/if}}
      <div class="row text-center">
        <div class="col-sm-12">
          <button href="#" class="btn btn-secondary btn-lg">
            No, thanks. Just let me in.
          </button>
        </div>
      </div>
    </div>
  </div>
</div>